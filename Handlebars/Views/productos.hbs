{{#if products}}

<br>
<h2 class="title">LISTA DE PRODUCTOS CON HANDLEBARS</h2>

<BR>
<table class="table">
        <thead>
            <tr class="bg-success">
            <th>Nombre</th>
            <th>Precio</th>
            <th>Imagen</th>
            </tr>
        </thead>
{{#each products}}
        <tbody>
            <tr class="background_table">
               <td>{{this.title}}</td>
               <td> {{this.price}}</td>
               <td><img srcSet={{this.thumbnail}} class="tamaño" sizes="(min-width: 10px)"/></td>                
            </tr>
        </tbody>
        {{/each}}
</table>

{{else}}
<h2 class="title">NO SE ENCONTRARON PRODUCTOS</h2>
{{/if}}
<br>
    <div class="center_button">
    <a href="/"><button class="btn_inicio">Volver a Inicio</button></a>
    </div>
